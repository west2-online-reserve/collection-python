## 全局公共参数

#### 全局Header参数

| 参数名 | 示例值 | 参数描述 |
|-----|-----|------|

暂无参数

#### 全局Query参数

| 参数名 | 示例值 | 参数描述 |
|-----|-----|------|

暂无参数

#### 全局Body参数

| 参数名 | 示例值 | 参数描述 |
|-----|-----|------|

暂无参数

#### 全局认证方式

```text
noauth
```

#### 全局预执行脚本

```javascript
暂无预执行脚本
```

#### 全局后执行脚本

```javascript
暂无后执行脚本
```

## /删除

```text
删除  对应id/已完成/待完成/所有事项
```

#### Header参数

| 参数名 | 示例值 | 参数描述 |
|-----|-----|------|

暂无参数

#### Query参数

| 参数名 | 示例值 | 参数描述 |
|-----|-----|------|

暂无参数

#### Body参数

| 参数名 | 示例值 | 参数描述 |
|-----|-----|------|

暂无参数

#### 认证方式

```text
noauth
```

#### 预执行脚本

```javascript
暂无预执行脚本
```

#### 后执行脚本

```javascript
暂无后执行脚本
```

## /删除/删除所有待办事项

```text
暂无描述
```

#### 接口状态

> 开发中

#### 接口URL

> 127.0.0.1:5000/del/all

#### 请求方式

> GET

#### Content-Type

> none

#### 认证方式

```text
noauth
```

#### 预执行脚本

```javascript
暂无预执行脚本
```

#### 后执行脚本

```javascript
暂无后执行脚本
```

#### 成功响应示例

```json
{
  "code": 200,
  "msg": "所有事项删除成功"
}
```

#### 错误响应示例

```json
{
  "code": 404,
  "msg": "操作失败"
}
```

## /删除/删除对应id待办事项

```text
暂无描述
```

#### 接口状态

> 开发中

#### 接口URL

> http://127.0.0.1:5000/del/id

#### 请求方式

> GET

#### Content-Type

> application/json

#### 请求Body参数

| 参数名 | 示例值 | 参数类型    | 是否必填 | 参数描述  |
|-----|-----|---------|------|-------|
| id  | -   | Integer | 是    | 事项的ID | 

#### 认证方式

```text
noauth
```

#### 预执行脚本

```javascript
暂无预执行脚本
```

#### 后执行脚本

```javascript
暂无后执行脚本
```

#### 成功响应示例

```json
{
  "code": 200,
  "msg": "删除成功"
}
```

#### 错误响应示例

```json
{
  "code": 404,
  "msg": "操作失败"
}
```

## /删除/删除待完成事项

```text
暂无描述
```

#### 接口状态

> 开发中

#### 接口URL

> 127.0.0.1:5000/del/completing

#### 请求方式

> GET

#### Content-Type

> application/json

#### 认证方式

```text
noauth
```

#### 预执行脚本

```javascript
暂无预执行脚本
```

#### 后执行脚本

```javascript
暂无后执行脚本
```

#### 成功响应示例

```json
{
  "code": 200,
  "msg": "待完成事项删除成功"
}
```

#### 错误响应示例

```json
{
  "code": 404,
  "msg": "操作失败"
}
```

## /删除/删除已完成事项

```text
暂无描述
```

#### 接口状态

> 开发中

#### 接口URL

> 127.0.0.1:5000/del/completed

#### 请求方式

> GET

#### Content-Type

> application/json

#### 认证方式

```text
noauth
```

#### 预执行脚本

```javascript
暂无预执行脚本
```

#### 后执行脚本

```javascript
暂无后执行脚本
```

#### 成功响应示例

```json
{
  "code": 200,
  "msg": "已完成事项删除成功"
}
```

#### 错误响应示例

```json
{
  "code": 404,
  "msg": "操作失败"
}
```

## /修改

```text
修改 对应id/所有待办事项 完成状态
```

#### Header参数

| 参数名 | 示例值 | 参数描述 |
|-----|-----|------|

暂无参数

#### Query参数

| 参数名 | 示例值 | 参数描述 |
|-----|-----|------|

暂无参数

#### Body参数

| 参数名 | 示例值 | 参数描述 |
|-----|-----|------|

暂无参数

#### 认证方式

```text
noauth
```

#### 预执行脚本

```javascript
暂无预执行脚本
```

#### 后执行脚本

```javascript
暂无后执行脚本
```

## /修改/修改所有待办事项

```text
将所有事项的完成状态修改为 completed(已完成) / completing(待完成)
```

#### 接口状态

> 开发中

#### 接口URL

> http://127.0.0.1:5000/modify/all

#### 请求方式

> POST

#### Content-Type

> application/json

#### 请求Body参数

| 参数名   | 示例值 | 参数类型   | 是否必填 | 参数描述                                 |
|-------|-----|--------|------|--------------------------------------|
| state | -   | String | 是    | 完成状态：completed(已完成)/ completing(待完成) |

#### 认证方式

```text
noauth
```

#### 预执行脚本

```javascript
暂无预执行脚本
```

#### 后执行脚本

```javascript
暂无后执行脚本
```

#### 成功响应示例

```json
{
  "code": 200,
  "msg": "修改成功"
}
```

#### 错误响应示例

```json
{
  "code": 404,
  "msg": "操作失败"
}
```

## /修改/修改id待办事项

```text
修改对应id事项的完成状态
```

#### 接口状态

> 开发中

#### 接口URL

> 127.0.0.1:5000/modify/id

#### 请求方式

> POST

#### Content-Type

> application/json

#### 请求Body参数

| 参数名   | 示例值 | 参数类型    | 是否必填 | 参数描述                                 |
|-------|-----|---------|------|--------------------------------------|
| id    | -   | Integer | 是    | 事项的ID                                |
| state | -   | String  | 是    | (完成状态)completed: 已完成；completing: 待完成 |

#### 认证方式

```text
noauth
```

#### 预执行脚本

```javascript
暂无预执行脚本
```

#### 后执行脚本

```javascript
暂无后执行脚本
```

#### 成功响应示例

```json
{
  "code": 200,
  "msg": "修改成功"
}
```

#### 错误响应示例

```json
{
  "code": 400,
  "msg": "操作失败"
}
```

## /查询

```text
查询  对应id/已完成/待完成/所有事项
```

#### Header参数

| 参数名 | 示例值 | 参数描述 |
|-----|-----|------|

暂无参数

#### Query参数

| 参数名 | 示例值 | 参数描述 |
|-----|-----|------|

暂无参数

#### Body参数

| 参数名 | 示例值 | 参数描述 |
|-----|-----|------|

暂无参数

#### 认证方式

```text
noauth
```

#### 预执行脚本

```javascript
暂无预执行脚本
```

#### 后执行脚本

```javascript
暂无后执行脚本
```

## /查询/查询所有待办事项

```text
暂无描述
```

#### 接口状态

> 开发中

#### 接口URL

> 127.0.0.1:5000/query/all

#### 请求方式

> POST

#### Content-Type

> application/json

#### 请求Body参数

| 参数名      | 示例值 | 参数类型    | 是否必填 | 参数描述      |
|----------|-----|---------|------|-----------|
| pagesize | -   | Integer | 是    | 每页事项条数数量  |
| page     | -   | Integer | 是    | 分页页码，数字类型 |

#### 认证方式

```text
noauth
```

#### 预执行脚本

```javascript
暂无预执行脚本
```

#### 后执行脚本

```javascript
暂无后执行脚本
```

#### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "1": {
      "id": 0,
      "state": "",
      "title": "",
      "content": "",
      "add_time": 20231105,
      "end_time": 20231106
    },
    "2": {
      "id": 1,
      "state": "",
      "title": "",
      "content": "",
      "add_time": 20231107,
      "end_time": 20231108
    }
  }
}
``` 

#### 错误响应示例

```json
{
  "code": 404,
  "msg": "操作失败"
}
```

## /查询/查询对应id待办事项

```text
暂无描述
```

#### 接口状态

> 开发中

#### 接口URL

> 127.0.0.1:5000/query/id

#### 请求方式

> POST

#### Content-Type

> application/json

#### 请求Body参数

| 参数名 | 示例值 | 参数类型   | 是否必填 | 参数描述  |
|-----|-----|--------|------|-------|
| id  | -   | String | 是    | 事项的ID | 

#### 认证方式

```text
noauth
```

#### 预执行脚本

```javascript
暂无预执行脚本
```

#### 后执行脚本

```javascript
暂无后执行脚本
```

#### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "id": 0,
    "state": "",
    "title": "",
    "content": "",
    "add_time": 0,
    "end_time": 0
  }
}
```

#### 错误响应示例

```json
{
  "code": 404,
  "msg": "操作失败"
}
```

## /查询/查询已完成事项

```text
暂无描述
```

#### 接口状态

> 开发中

#### 接口URL

> 127.0.0.1:5000/query/completed

#### 请求方式

> POST

#### Content-Type

> application/json

#### 请求Body参数

| 参数名      | 示例值 | 参数类型    | 是否必填 | 参数描述      |
|----------|-----|---------|------|-----------|
| page     | -   | Integer | 是    | 分页页码，数字类型 |
| pagesize | -   | Integer | 是    | 每页事项条数数量  |

#### 认证方式

```text
noauth
```

#### 预执行脚本

```javascript
暂无预执行脚本
```

#### 后执行脚本

```javascript
暂无后执行脚本
```

#### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "1": {
      "id": 0,
      "state": "completed",
      "title": "",
      "content": "",
      "add_time": 20231105,
      "end_time": 20231106
    },
    "2": {
      "id": 1,
      "state": "completed",
      "title": "",
      "content": "",
      "add_time": 20231107,
      "end_time": 20231108
    }
  }
}
```

#### 错误响应示例

```json
{
  "code": 404,
  "msg": "操作失败"
}
```

## /查询/查询待完成事项

```text
暂无描述
```

#### 接口状态

> 开发中

#### 接口URL

> 127.0.0.1:5000/query/completing

#### 请求方式

> POST

#### Content-Type

> application/json

#### 请求Body参数

| 参数名      | 示例值 | 参数类型   | 是否必填 | 参数描述      |
|----------|-----|--------|------|-----------|
| page     | -   | String | 是    | 分页页码，数字类型 |
| pagesize | -   | String | 是    | 每页事项条数数量  |

#### 认证方式

```text
noauth
```

#### 预执行脚本

```javascript
暂无预执行脚本
```

#### 后执行脚本

```javascript
暂无后执行脚本
```

#### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "1": {
      "id": 0,
      "state": "completing",
      "title": "",
      "content": "",
      "add_time": 20231105,
      "end_time": 20231106
    },
    "2": {
      "id": 1,
      "state": "completing",
      "title": "",
      "content": "",
      "add_time": 20231107,
      "end_time": 20231108
    }
  }
}
```

#### 错误响应示例

```json
{
  "code": 404,
  "msg": "操作失败"
}
```

## /查询/查询关键字事项

```text
暂无描述
```

#### 接口状态

> 开发中

#### 接口URL

> 127.0.0.1:5000/query/key

#### 请求方式

> POST

#### Content-Type

> application/json

#### 请求Body参数

| 参数名      | 示例值 | 参数类型      | 是否必填 | 参数描述      |
|----------|-----|-----------|------|-----------|
| key      | -   | String    | 是    | 关键字       |
| value    | -   | Undefined | 是    | 关键字的值     |
| page     | -   | String    | 是    | 分页页码，数字类型 |
| pagesize | -   | String    | 是    | 每页事项条数数量  |

#### 认证方式

```text
noauth
```

#### 预执行脚本

```javascript
暂无预执行脚本
```

#### 后执行脚本

```javascript
暂无后执行脚本
```

#### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "1": {
      "id": 0,
      "state": "",
      "title": "",
      "content": "",
      "add_time": 20231105,
      "end_time": 20231106
    },
    "2": {
      "id": 1,
      "state": "",
      "title": "",
      "content": "",
      "add_time": 20231107,
      "end_time": 20231108
    }
  }
}
```

#### 错误响应示例

```json
{
  "code": 404,
  "msg": "操作失败"
}
```

## /添加

```text
添加 一条待办事项
```

#### Header参数

| 参数名 | 示例值 | 参数描述 |
|-----|-----|------|

暂无参数

#### Query参数

| 参数名 | 示例值 | 参数描述 |
|-----|-----|------|

暂无参数

#### Body参数

| 参数名 | 示例值 | 参数描述 |
|-----|-----|------|

暂无参数

#### 认证方式

```text
noauth
```

#### 预执行脚本

```javascript
暂无预执行脚本
```

#### 后执行脚本

```javascript
暂无后执行脚本
```

## /添加/添加代办事项

```text
添加一条待办事项
```

#### 接口状态

> 开发中

#### 接口URL

> http://127.0.0.1:5000/add

#### 请求方式

> POST

#### Content-Type

> application/json

#### 请求Body参数

| 参数名      | 示例值 | 参数类型    | 是否必填 | 参数描述                                |
|----------|-----|---------|------|-------------------------------------|
| id       | -   | Integer | 是    | 事项的ID                               |
| title    | -   | String  | 是    | 标题                                  |
| content  | -   | String  | 是    | 内容                                  |
| add_time | -   | Integer | 是    | 添加时间                                |
| end_time | -   | Integer | 是    | 结束时间                                |
| state    | -   | String  | 是    | 完成状态：completed(已完成)/completing(待完成) |

#### 认证方式

```text
noauth
```

#### 预执行脚本

```javascript
暂无预执行脚本
```

#### 后执行脚本

```javascript
暂无后执行脚本
```

#### 成功响应示例

```json
{
  "code": 200,
  "msg": "添加成功"
}
```
#### 错误响应示例

```json
{
 "code": 404,
  "msg": "操作失败"
}
```